---
import { parse } from "marked";
interface Props {
	content: string;
    classN?: string;
    textAlign?: string;
}

const { content, classN, textAlign="text-justify" } = Astro.props;
console.log(textAlign);
---
    <div set:html={parse(content)} class={`${textAlign} flex list-outside flex-col gap-3 text-primary-blue [&>*>strong]:text-sky-500 [&>*>a]:text-secondary-red [&>h1]:text-[36px] [&>h1]:font-[600] [&>h1]:text-[#50d71e]  [&>h2]:text-[32px] [&>h3]:text-[26px] [&>ul]:flex [&>ul]:flex-col [&>ul]:gap-3 ${classN} `}/>
