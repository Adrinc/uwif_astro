import { BrowserRouter } from 'react-router-dom'

import TopMenu from './TopMenu';

function RoterLinks() {
  return (
    <BrowserRouter>
      <TopMenu />
    </BrowserRouter>
  );
}

export default RoterLinks;