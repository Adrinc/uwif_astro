---

import {strapiURL} from '../../../services/graphql';


interface Props{
    strapiData: any;
}
const{ strapiData } = Astro.props;


const bento = strapiData['pageData']['data']['pageHome']['data']['attributes']['BentoGrid'];
---

<section id="#bentogrid" class="">


    <img class="web hidden md:block" src=`${strapiURL}${bento.data[0].attributes.url}` alt={bento.data[0].attributes.name}>
    <img class="tablet hidden sm:block md:hidden" src="/image/bento/btablet.png" alt="">
    <img class="movil block sm:hidden" src=`${strapiURL}${bento.data[1].attributes.url}` alt={bento.data[1].attributes.name}>
<!--     <img class="tablet hidden sm:block md:hidden" src=`${strapiURL}${bento.data[2].attributes.url}` alt={bento.data[2].attributes.name}> -->

</section>
		  
	<style>


section{
		
        display: flex;
        flex-direction: column;
        justify-content: center;
        box-sizing: border-box;
        padding-left: clamp(0px, 5vw, 100px);
        padding-right: clamp(0px, 5vw, 100px);
        margin-top: 100px;
        margin-bottom: 100px;
        height: 80vh;
        
    }
    img{
        width: 100%;
        height: 100%;
    }

        @media (max-width: 768px) {
          
        }
    

	
 </style>