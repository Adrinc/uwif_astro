---

---
<section>
   
    <p class="title">FREE <span class="text-primaryTextColor"> TABLET!*</span></p>

    <img src="/image/uwifitablet.png" alt="tablet" class="image"/>
    <span class="green-ball-blured"></span>
    <p class="parrafo">*Free tablet if you are subject to the ACP program</p>
</section>
 
<style>
    section{
    position: relative;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    align-items: center;
    padding-top: clamp(0px, 10vw, 70px);
    padding-bottom: clamp(0px, 10vw, 70px);
    padding-left: clamp(0px, 10vw, 100px);
    padding-right: clamp(0px, 10vw, 100px);
 
    }
    .green-ball-blured{
    top:20%;
	left: 35%;
	position: absolute;
    width: clamp(0px, 50vw, 530px);
	height: clamp(0px, 50vw, 530px);
	border-radius: 100%;
    background: linear-gradient(0deg, #03FB75 0%, #03FB75 100%);
	filter: blur(100px);
    opacity: 0.8;
	z-index: -1;
	}
	.title{
	color: var(--primary-color);
	font-size: clamp(14px, 50vw, 75px);
	font-weight: 700;
	text-align: center;
	justify-content: center;
	}
    .image {
	display: inline-block;
	width: clamp(30px, 50vw, 500px); 
	height: clamp(30px, 50vw, 500px);
    align-self: center;
    margin-bottom: 25px;
	}
    .parrafo{
    color: var(--primary-text-color);
    font-size: clamp(14px, 3vw, 24px);
    opacity: 0.6;
    text-align: center;
    }
    @media (max-width: 768px) 
    {
		
	.title{
	color: var(--primary-color);
	font-size: clamp(14px, 30vw, 45px);
	font-weight: 700;
	text-align: center;
	justify-content: center;
	}
    .parrafo{
    color: var(--primary-text-color);
    font-size: clamp(14, 3vw, 18px);
    opacity: 0.6;
    }
	}
    
</style>